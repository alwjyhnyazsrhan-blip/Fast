<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- حماية اتصالات الـ API وفرض تشفير HTTPS ومنع اعتراض البيانات (MitM) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- استثناء محدد للاختبارات المحلية فقط على أجهزة المحاكاة (Emulator Loopback) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- فرض تشفير صارم لسيرفرات Render و Google Cloud السحابية -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">onrender.com</domain>
        <domain includeSubdomains="true">railway.app</domain>
        <domain includeSubdomains="true">run.app</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
